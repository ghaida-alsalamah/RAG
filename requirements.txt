# GoRiyadh – RAG Tourist Guide
# Install with: pip install -r requirements.txt

# Data
pandas>=2.0
openpyxl>=3.1

# Embeddings (torch CPU-only — no CUDA needed)
torch --index-url https://download.pytorch.org/whl/cpu
sentence-transformers>=2.7

# Vector database
faiss-cpu>=1.7

# LLM (Groq cloud — no local model needed)
groq>=0.9
python-dotenv>=1.0

# API server
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9
